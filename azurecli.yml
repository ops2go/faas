provider:
  name: faas
  gateway: http://localhost:8080

functions:
  awscli:
    lang: Dockerfile
    handler: ./awscli
    image: colemanword/azurecli:faas
    environment:
      AZURE_TENANT_ID	: value1
      AZURE_CLIENT_ID:	value2
      AZURE_USERNAME:	value3
      AZURE_PASSWORD: value4
